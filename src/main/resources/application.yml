server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/KoH-IS/config-data.git
          search-paths: api
          default-label: "master"
          
  security:
    user:
      name: root
      password: adminadmin   